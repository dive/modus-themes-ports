# AGENTS

- Use `python3 scripts/modus.py` for all tasks.
- Tool registry lives in `ports/*/*-port.json`.
- Templates are in `ports/<tool>/theme.tmpl`.
- Development instructions are in `CONTRIBUTING.md`.
- Keep `README.md` minimal; move dev details to `CONTRIBUTING.md`.
